package io.springmvc.springmvcmaster.controller;import io.springmvc.springmvcmaster.dto.User;import org.springframework.stereotype.Controller;import org.springframework.ui.Model;import org.springframework.web.bind.annotation.*;import org.springframework.web.bind.support.SessionStatus;import java.util.Objects;@Controller@RequestMapping("/user")@SessionAttributes("user")public class UserController {    @ModelAttribute    public User addUser(){       return new User("springmvc", "a@a.com");    }    @GetMapping("")    public String users(Model model) {//        model.getAttribute("user", new User("springmvc", "a@a.com"));        return "redirect:/user/filter";    }    @GetMapping("/filter")    public String getUsers(@ModelAttribute("user") User user, Model model) {        if (Objects.isNull(user)) {            return "redirect:/user/notFound";        }        return "userDetail";    }    @GetMapping("/notFound")    public String userNotFound(){        return "userNotFound";    }    @PostMapping("/session/clear")    public String clearSession(SessionStatus sessionStatus){        sessionStatus.setComplete();        return "redirect:/user/form";    }    @GetMapping("/form")    public String showForm() {        return "userForm";    }}